package io.github.lukeeff.spigottemplate.hud;


import io.github.lukeeff.spigottemplate.HUDLearning;
import lombok.Getter;
import net.minecraft.server.v1_15_R1.*;
import org.bukkit.Color;
import org.bukkit.Effect;
import org.bukkit.craftbukkit.v1_15_R1.entity.CraftPlayer;
import org.bukkit.map.MapView;
import org.bukkit.potion.PotionEffect;
import org.bukkit.potion.PotionEffectType;
import org.bukkit.potion.PotionEffectTypeWrapper;

public class Learning {
/*
    @Getter private final HUDLearning plugin;
    @Getter private final ProtocolManager manager;

    public Learning(HUDLearning plugin) {
        this.plugin = plugin;
        this.manager = plugin.getProtocolManager();

    }

    private void customGUI() {
        getManager().addPacketListener(new PacketAdapter(getPlugin(), PacketType.Play.Server.OPEN_WINDOW) {
            @Override
            public void onPacketReceiving(PacketEvent event) {
                //event.getPacket().
                //GuiStatsComponent.

                //NBTTagCompound.





                super.onPacketReceiving(event);
            }
        });
    }


    private void getPotionWithCustomName(PacketEvent event) {



        PotionEffectType effect = new PotionEffectType(2) {
            @Override
            public double getDurationModifier() {
                return 1;
            }

            @Override
            public String getName() {
                return "test";
            }

            @Override
            public boolean isInstant() {
                return false;
            }

            @Override
            public Color getColor() {
                return PotionEffectType.ABSORPTION.getColor();
            }
        };





    }
*/

}
